dist: trusty
language: csharp
solution: PESEL.sln
install:
  - nuget restore PESEL.sln
script:
  - xbuild /p:Configuration=Release PESEL.sln /p:TargetFrameworkVersion="v4.7.2"
  - sonar-scanner
  
addons:
  sonarcloud:
    organization: "asienicki-github"
    token:
      secure: $h
